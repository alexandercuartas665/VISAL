namespace Visal.Domain.Enums;

/// <summary>Proveedor de lectura de correo entrante para ingesta de PQR.</summary>
public enum EmailIngestProvider
{
    /// <summary>IMAP (Gmail, etc.) autenticado con App Password.</summary>
    Imap = 0
}
